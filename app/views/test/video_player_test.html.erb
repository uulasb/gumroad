<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>Video Player Test</title>

  <% if params[:fake].to_s == '1' %>
    <script src="/assets/fake_jwplayer.js"></script>
  <% else %>
    <script src="https://ssl.p.jwpcdn.com/player/v/8.29.0/jwplayer.js" crossorigin="anonymous"></script>
  <% end %>

  <%# cmp=1 renders the real TSX component; otherwise use the plain test glue %>
  <% if params[:cmp].to_s == '1' %>
    <%= javascript_include_tag "video_stream_player_test", defer: true %>
  <% else %>
    <%= javascript_include_tag "test_video_player", defer: true %>
  <% end %>
</head>
<body>
  <% if params[:cmp].to_s == '1' %>
    <div id="video-stream-player-root" style="max-width:800px;margin:20px auto;"></div>
  <% else %>
    <div id="gumroad-player" style="max-width:800px;margin:20px auto;"></div>
  <% end %>
</body>
</html>